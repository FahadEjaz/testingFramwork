services:
  tests:
    build: .
    image: testing-framework-v2:local
    ipc: host
    environment:
      - CI=${CI:-}
      - BASE_URL=${BASE_URL:-}
    volumes:
      - ./playwright-report:/app/playwright-report
      - ./test-results:/app/test-results
    command: ["npx", "playwright", "test"]
